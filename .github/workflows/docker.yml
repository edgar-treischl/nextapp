# Create and switch to gh-pages
git checkout --orphan gh-pages

# Remove old files (if any)
git rm -rf .

# Copy the contents of out/
cp -r out/* .

# Add and commit
git add .
git commit -m "Deploy Next.js static site"

# Push to GitHub
git push origin gh-pages --force
